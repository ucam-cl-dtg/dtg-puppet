# example Monkeysphere cron job:

# Hourly: update the per-user authorized_keys in /var based on
# ~/.monkeysphere/authorized_user_ids

<%= ms_min %> * * * * root /usr/sbin/monkeysphere-authentication update-users 2>/dev/null
